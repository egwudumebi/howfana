# Paystack (test keys — never commit secret keys to the app)
EXPO_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_your_key_here

# For EAS Build preview/production, set the Paystack public key as an EAS environment variable:
#   npx eas-cli env:set preview --name EXPO_PUBLIC_PAYSTACK_PUBLIC_KEY --value pk_test_... --visibility plaintext --type string
